<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/all.css" integrity="sha384-vp86vTRFVJgpjF9jiIGPEEqYqlDwgyBgEF109VFjmqGmIY/Y4HV4d3Gp2irVfcrp" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/register.css">
    <link rel="apple-touch-icon" sizes="180x180" href="/icon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/icon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/icon/favicon-16x16.png">
    <link rel="manifest" href="/icon/site.webmanifest">

    <title>Registrate en GoodBikes</title>
</head>
<body>
  <div id='Contenedor_Body'>  
    <%- include('../partials/header')%>

    <img id='img_login' src='/images/register.png'>

    <div class="contenedor_del_Main"> 


      <main>
        <form action="" method="POST" enctype="multipart/form-data">
          <div class="datos">
            <h1>Registrate en GoodBikes</h1>
            <input type="text" name="name" id="name" placeholder="Nombre" value="<%=(typeof old != 'undefined' && !errors.name) ? old.name : '' %>">
            <small class="errores">
              <%=(locals.errors && errors.name) ? errors.name.msg : "" %> 
            </small>
            <input type="text" name="apellido" id="apellido" placeholder="Apellido" value="<%=(typeof old != 'undefined' && !errors.apellido) ? old.apellido : '' %>">
            <small class="errores">
              <%=(locals.errors && errors.apellido) ? errors.apellido.msg : "" %> 
            </small>
            <input type="email" name="email" id="email" placeholder="Email" value="<%=(typeof old != 'undefined' && !errors.email) ? old.email : '' %>">
            <small class="errores">
              <%=(locals.errors && errors.email) ? errors.email.msg : "" %> 
            </small>
            <!-- <input type="text" name="dni" id="dni" placeholder="DNI"> -->
            <input type="password" name="password" id="password" placeholder="Password" value="<%=(typeof old != 'undefined' && !errors.password) ? old.password : '' %>">
            <small class="errores">
              <%=(locals.errors && errors.password) ? errors.password.msg : "" %> 
            </small>
            <input type="password" name="repassword" id="repassword" placeholder="Repetir Password">
            <label id="labelAvatar" for="">Imagen de perfil</label>
            <input id="avatar" type="file" name="avatar">
            <button type="submit">Crear cuenta</button>
          </div>
       </form>
      </main>
    </div>
    
    <%- include('../partials/footer')%>

  </div>
</body>
</html>